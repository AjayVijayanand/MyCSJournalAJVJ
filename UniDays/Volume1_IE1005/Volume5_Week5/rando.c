/* Example on the use of rand() */
#include <stdio.h>
#include <stdlib.h> //Library for rand()
int main(void) {
    printf("Three random integers are:\n %d %d %d\n", rand(), rand(), rand());
    return 0;
}